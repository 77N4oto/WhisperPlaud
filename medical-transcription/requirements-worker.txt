# Medical Transcription Worker Dependencies - Minimal
# Core dependencies
redis>=5.0.0
sqlalchemy>=2.0.0
boto3>=1.34.0
requests>=2.31.0
python-dotenv>=1.0.0

# BullMQ Python worker (no official client, we'll use redis directly)
# Using taskiq-redis as it provides BullMQ compatible worker pattern
taskiq[redis]>=0.11.0

# Audio processing - lightweight  
# Note: Uncomment for production use (large downloads)
# torch>=2.1.0 --index-url https://download.pytorch.org/whl/cpu
# faster-whisper>=1.0.0
# openai-whisper>=20231117

# Utilities
numpy>=1.24.0
